name: gie_prouits
description: "Gestion d'un GIE féminin de trqnsfor,qtion de produits locaux ODD 5"
publish_to: 'none'
versions: 1.0.0+1


environment:
  sdk: '>=3.0.0 <4.0.0'


dependencies:
  flutter: 
    sdk: flutter
  intl: ^0.19.0 # permet la formatage des dates en francais
  image_picker: ^1.1.2  # upload d'image depuis la galerie


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/images/    # c ici qu'on met les images